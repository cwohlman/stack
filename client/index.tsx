import React from 'react';
import ReactDOM from 'react-dom';
import HomePage from '../features/HomePage/HomePageView';

const root = document.querySelector('#root');
ReactDOM.render(<HomePage ports={{} as any} />, root);
